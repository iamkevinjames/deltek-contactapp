export interface ContactResponseModal {
  id: string;
  contactName: string;
  contactNumber: string;
  location: string;
}

export interface ContactRequestModal {
  contactName: string;
  contactNumber: string;
  location: string;
}
